# i3status configuration file.

general {
        colors = true
        interval = 1
}

order += "load"
order += "tztime local"

tztime local {
        format = "%a %b %d %H:%M:%S"
}

load {
        format = "%1min"
}
